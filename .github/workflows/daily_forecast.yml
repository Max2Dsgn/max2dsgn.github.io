name: 🕓 Daily AI Forecast

on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

permissions:
  contents: write   # важно для возможности push

jobs:
  update_forecast:
    runs-on: ubuntu-latest

    steps:
      - name: 🧩 Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: true   # позволяет использовать GITHUB_TOKEN для push

      - name: 🐍 Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: ⚙️ Run forecast script
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python generate_forecast.py

      - name: 🐙 Git diagnostics (optional, remove later)
        run: |
          echo "=== git status ==="
          git status --porcelain=2 --branch || true
          echo
          echo "=== git remote -v ==="
          git remote -v || true
          echo
          echo "=== git config ==="
          git config --list || true

      - name: Commit and push changes
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global user.email "bot@forecast.ai"
          git config --global user.name "AI Forecaster Bot"
          git add data/forecasts_history.json
          git commit -m "🤖 Auto-update forecast $(date +'%Y-%m-%d')" || echo "No changes to commit"
          git push https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }} HEAD:main
